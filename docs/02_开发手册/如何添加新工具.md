# 如何添加新工具

## 步骤

### 1. 创建工具模块

在 `01_工具模块/` 下创建子目录，实现 `process_command()` 函数：

```python
# 01_工具模块/天气模块/weather_module.py

def process_command(text):
    """解析自然语言指令并返回结果"""
    if "天气" in text:
        return "今天晴，25°C"
    return "请说明要查询天气的城市"
```

### 2. 注册到工具分发器

在 `00_核心主体/器官/手.py` 的 `TOOL_MODULES` 中注册：

```python
TOOL_MODULES = {
    "财务": "财务模块.finance_module",
    "日程": "日程模块.schedule_module",
    "天气": "天气模块.weather_module",  # 新增
}
```

### 3. 注册到路由层

在 `脑.py` 的 `TOOLS` 列表中注册：

```python
TOOLS = [
    {
        "name": "天气",
        "desc": "天气查询",
        "signals": "今天天气、明天天气、会下雨吗",
    },
    # ... 其他工具
]
```

LLM 路由层会自动将包含天气关键词的输入路由到新工具。

### 4. （可选）添加结构化参数入口

如果路由层需要直接传递结构化参数而非原始文本：

```python
# 在模块中实现
def get_weather(city):
    # ...

# 在 脑.py 中通过 _func 参数调用
call_tool("天气", city, _func="get_weather")
```

### 5. 更新文档

在 `docs/01_功能手册/` 中为新模块创建说明文件。
