# 晓风Agent · 项目状态

**当前版本：v3.13.20** — 2026-08-25

> 📚 **文档已迁移至 `docs/` 目录。**  
> 请从 [`docs/README.md`](docs/README.md) 开始阅读。  
> 旧版完整状态文件已归档到 `docs/99_归档/PROJECT_STATUS_OLD.md`。

## 当前状态

| 项目 | 状态 |
|------|------|
| 路由层 | ✅ 3b 紧凑路由优先（v3.9.0，<3s）→ 7b 降级 → 关键词兜底；v3.9.27 增加确定性预路由 + 多级兜底 |
| 回复层 | ✅ 统一工具回复 + 自动追问（v3.9.23）+ 对话修复 v3.13.16（问候语明确"我能帮你做什么"+ "你说什么/再说一遍"重复上一轮回复）+ v3.13.17（待记账状态机："帮我记个账"回"好的，你说我听着呢"待补充金额；"你说什么+内容"自然提问交 7b 回答） |
| 日程模块 | ✅ 可用（30s 轮询守护提醒 + 模糊删除 + 修正机制 + 15 字段存储标准化 v3.9.25 + 路由鲁棒性修复 v3.9.27 + 删除序号/自然表达修复 v3.9.28 + 查询/删除/修正修复 v3.13.8：显示待办误判/代办触发词/日程按时间删除/把它改成修正） |
| 财务模块 | ✅ 可用（15 字段记录标准化 + 分类/时段/排序 + LLM 日期解析 + 查询上下文继承 + 幻觉记账修复 v3.10.10：保存后验证落盘 + 失败返回错误 + 查询修复 v3.13.2：范围词映射/来源过滤/闭区间 + v3.13.3：收支类型区分 + v3.13.4：收入模块（来源汇总/收支明细/收入趋势）+ v3.13.14：详情/展开上下文继承（"详细一点"明细触发补"展开"）+ v3.13.15：继承判定稳健化（改用 `_last_query_context`）+ v3.13.18/19：财务报告生成（`generate_report` 四段式：收支汇总 → 交易明细 → 期间对比 → 分类统计，纯 Python 聚合无 LLM；v3.13.19 起支持 `scope` 参数——默认=今日汇总（无则最近有数据日）+近3天明细(≤30)+近3天分类+全量期间对比，另含 today/week/month/all）+ v3.13.20：Phase 3 图表 + Excel（`generate_report` 新增 `format` 参数 docx/excel/both——Word 五段式插入收支趋势图表（matplotlib，近7天/近30天 × 折线/柱状，支出/收入/结余三指标，位于期间对比表前）；新增 `generate_excel` 导出四工作表 Excel（Summary/Category/Comparison/Detail）；matplotlib 缺失跳过图表注明不阻塞、openpyxl 缺失 "excel"/"both" 报错（both 仍返回 Word 部分成功）；临时图表文件自动清理）） |
| 记忆系统 | ✅ 框架搭建完成，facts/patterns/短期记忆/摘要/衰减归档 + 15 字段统一记忆库 v3.9.29 + 记忆路由修复 v3.9.30 + 确定性记忆预路由 v3.9.31 + **对话归档 v3.10.0（Phase 1）+ 渐进披露 v3.10.2（Phase 2）+ 标签生成与时间索引 v3.10.7（Phase 3）+ 置信度衰减与记忆整合 v3.10.8（Phase 4：tier L1/L2 + 日衰减 + 检索提升 + 归档 + 每日维护）+ 测试修复 v3.10.9（财务记忆写入硬化 + 搜索噪音过滤）+ 对比分析 v3.11.0（Phase 5：跨期对比/分类/最大开支/高频活动/消费时段/周总结，`run_analysis` 统一下发 + 「分析」意图四级路由）+ 情绪检测与记忆关联 v3.12.0（Phase 6：`detect_mood` 9 类情绪 hybrid 检测 + `related_ids` 知识图谱关联 + 预路由 (J) 关联记忆查询）+ 体验修复 v3.12.1（强情绪自动创建情绪记忆 + 关联记忆结构化返回与显示）+ 情绪路由修复 v3.12.2（预路由 (K) 情绪表达 → chat 绕过 3b + 关联查询 live 实时兜底）+ 增强对比分析 v3.13.0（Phase 7：习惯总结 `get_habit_summary` + 消费趋势 `analyze_spending_trend` + 个性化建议 `get_personalized_recommendations`，分析路由触发词扩展 习惯/趋势/建议）+ 身份查询修复 v3.13.1（预路由 (B) 复用 `get_user_name` 全量检索，修复名字事实在 top-50 之外时"我是谁"查不到）+ 关键词搜索格式化 v3.13.9（Phase 7 Fix A：`format_keyword_search` 分组摘要/`_display_text` 内容化展示/过滤确认回声与列表 dump 噪音）+ 时间回查摘要 v3.13.10（Phase 7 Fix B：`format_time_recall` 分组计数——记账N笔共X元/日程待办计数/事实列出）+ 裸时间词路由修复 v3.13.11（预路由 (I) 支持 本月/本周/今天/昨天/上个月 → 时间回查摘要；排除财务词）+ 记忆查询展示格式化 v3.13.12（Phase 7 Fix C：`format_memory_query` 按类型分组——事实/事件/情绪/习惯，事件内财务合并"记账N笔共X元"，情绪显示"开心（心情很好）"）+ 关联查询可读输出 v3.13.13（Phase 7 Fix D：`format_related_query` 按强度分组/内容+领域+原因/过滤通用回声）** |
| 语音交互 | ✅ 可用（VAD 动态录音 + AGC + Vosk 离线识别 + Edge TTS 合成） |
| 拟态前端 | 🔀 已于 2026-07 分离为独立仓库 → [github.com/ZSL410/nitai](https://github.com/ZSL410/nitai) |

## 最近版本

| 版本 | 日期 | 核心变更 |
|------|------|----------|
| v3.13.20 | 2026-08-25 | ⚡ 财务报告 Phase 3：`generate_report` 新增 `format` 参数（docx/excel/both）+ Word 报告插入收支趋势图表（matplotlib 近7天/近30天 折线/柱状，支出/收入/结余三指标）+ 新增 `generate_excel` 四工作表导出（Summary/Category/Comparison/Detail）；matplotlib/openpyxl 缺失优雅降级（both 部分成功）；临时图表自动清理。**Phase 3 完成** |
| v3.13.19 | 2026-08-25 | 🐛/🧹 财务报告 Phase 2.5：`generate_report` 默认范围修正（今日汇总+近3天明细/分类+全量对比）+ 新增 `scope` 参数（today/week/month/all）；旧版独立 `generate_report.py` 归档到 `docs/99_归档/` |
| v3.13.18 | 2026-08-25 | ⚡ 财务报告 Phase 2：`generate_report` 四段式——收支汇总 + 分类统计（分类/金额/占比）+ 期间对比（本周vs上周、本月vs上月）+ 交易明细（最近50条），纯 Python 聚合无 LLM（Phase 1 基础上扩展） |
| v3.13.17 | 2026-08-24 | 🐛 待记账状态机 + 自然提问预路由："你好，帮我记个账"→"好的，你说我听着呢"待补金额（下轮补金额即记账/取消词取消/查询放行）；"你说什么颜色的好"→7b 自然回答（与 v3.13.16 澄清重复互补） |
| v3.13.16 | 2026-08-24 | 🐛 对话修复：问候语明确"我可以帮你做什么"（系统提示词风格指南 + 纯问候语确定性回复）；"你说什么/再说一遍/我没听清"重复上一轮回复并追问"需要我再说一遍吗？"（不再落"不太确定你的意思"兜底） |
| v3.13.15 | 2026-08-23 | 🐛 详情/展开继承稳健化：预路由 (Q) 改用财务模块 `_last_query_context` 判定（3b target 判 null 时不再失效）；无财务上下文回退日程继承 |
| v3.13.14 | 2026-08-23 | 🐛 详情/展开上下文继承：预路由 (Q) 裸详情词（详细一点/展开讲讲）→ 继承财务上下文+明细；财务 DETAIL 触发词补"展开"；(H) 展开不再回显原文 |
| v3.13.13 | 2026-08-22 | ⚡ 关联查询可读输出（Phase 7 Fix D）：`format_related_query` 按强度分组（强/中/弱）、`_display_text` 内容 + 领域标签 + 原因、过滤通用回声；预路由 (J) 改用格式化输出。**Phase 7 四 Fix 完成** |
| v3.13.12 | 2026-08-22 | ⚡ 记忆查询展示格式化（Phase 7 Fix C）：`format_memory_query` 按类型分组（事实/事件/情绪/习惯，事件内财务合并）；情绪显示"开心（心情很好）"；预路由 (D) 与 3b 镜像改用分组展示；搜索噪音过滤补摘要回声 |
| v3.13.11 | 2026-08-22 | 🐛 裸时间词路由修复：预路由 (I) 支持 本月/本周/今天/昨天/上个月 → 时间回查摘要（不再默认财务查询）；排除 花了/消费/收入/支出（财务查询不被劫持）；`_date_range_for` 补 last_month |
| v3.13.10 | 2026-08-22 | ⚡ 时间回查摘要（Phase 7 Fix B）：`format_time_recall` 分组计数——「记账N笔共X元 / 添加日程N个 / 添加待办N个 / 事实列出」，替代原始记录 dump；limit 200 计数准确 + 检索提升限前10条 |
| v3.13.9 | 2026-08-21 | ⚡ 关键词搜索格式化（Phase 7 Fix A）：回答生成层——分组摘要（事实/财务/日程/待办/情绪/对话）、`_display_text` 用 content/detail 替代泛化 title、过滤确认回声与长列表 dump 噪音 |
| v3.13.8 | 2026-08-19 | 🐛 日程模块修复："显示所有待办"查询排除词（"所有"的"有"误匹配）+ "代办"补触发词 + 预路由 (N) 扩展日程按时间删除（先确认）+ 预路由 (O) 提醒修正（把它改成X分钟/点/半小时）+ 预路由 (P) 裸日程查询 |
| v3.13.7 | 2026-08-16 | 🐛 删除确认 + 删除路由 + 语音提示：确定性删除预路由 (N)（"删除昨天"不再误判记账）+ 高风险删除确认状态机 + 3b 删除路径确认 + `delete_by_scope` 新增 yesterday 范围 + V 键「正在听」提示与异常兜底 |
| v3.13.6 | 2026-08-16 | 🐛 明细收支分计 + 收入确认措辞：明细模式全量查询输出「支出合计X元（N笔）；收入合计Y元（M笔）」；`_fmt_record` 传 `result["type"]` 修复收入确认语仍显「花的」 |
| v3.13.5 | 2026-08-16 | 🐛 收支方向继承 + 收入确认措辞：上下文继承新增 `direction`（「把它们详细列出来」沿用上一轮收入方向）+ `_detect_direction` 抽取 + 收入记账确认语「收入X元」修复（原「收入花的X元」） |
| v3.13.4 | 2026-08-16 | ⚡ 收入模块完善：「收入来源有哪些」「最大收入来源」来源分组汇总 +「查看所有财务数据」收支明细（支出X笔A元；收入Y笔B元）+ `get_income_total`/`get_income_sources`/`get_income_trend` 三接口 + 预路由 (M) 收入查询确定性路由（含「收入趋势」） |
| v3.13.3 | 2026-08-16 | 🐛 收支类型区分：`_execute_query` 按查询原文判定方向（收入/支出/全量，默认支出），聚合前按 `type` 过滤 →「今天花了多少」不再把收入计入（6笔5066元→5笔66元+「另有1笔收入5000元」标注）；「查看所有财务数据」仍返回收支全量 |
| v3.13.2 | 2026-08-16 | 🐛 财务查询修复：①范围词映射 `date_range`（本周/上周/本月/上个月不再被当精确单日，"本周花了多少"现返回整周 22 条）②`_extract_source_hint` 确定性来源兜底（"吃饭花了多少"从 71 条 → 饭 18 条）③时间范围闭区间化（修复 `last_month` 英文码跨月，"上个月"现仅返回 7 月 41 条） |
| v3.13.1 | 2026-08-16 | 🐛 身份查询修复：预路由 (B)「我是谁/我叫什么」改复用 `get_user_name()`（全量检索）——旧实现内联 `get_memories(limit=50)` 只查最近 50 条，名字事实通常早期写入（本例 rank#52）落在窗口外 → 返回"不确定"；写入侧确认正常（facts.json + memory.json 均含"我叫霖"事实） |
| v3.13.0 | 2026-08-16 | ⚡ 记忆系统 Phase 7（增强对比分析）：①习惯总结 `get_habit_summary`（patterns.json 行为模式 + 高频活动三源合并，含「已持续N天/周」时长，排除情绪/聊天非行为标签）②消费趋势 `analyze_spending_trend`（按周聚合最近3时段 ±10% 判 上升/下降/平稳 + 分类增长洞察，数据不足两周降级按月）③个性化建议 `get_personalized_recommendations`（咖啡→自制咖啡 / 外卖→自己做饭 / 交通→公共交通，每条含 建议+理由+每月节省估算；无命中兜底预算建议）+ `run_analysis` 新增 习惯/趋势/建议 三分发 + 脑.py「分析」四级路由触发词扩展，版本升至 v3.13.0 |
| v3.12.2 | 2026-08-16 | 🐛 情绪路由修复："我今天心情很好"不再被 3b 误判为财务（新增预路由 (K) 情绪表达 → chat，情绪词触发 + 搜索/记账/日程等工具意图排除；3b 聊天触发词补情绪表达）+ `get_related_memories(live=True)` 实时兜底（"咖啡和什么相关"在无已存储关联时现算，总有结果） |
| v3.12.1 | 2026-08-16 | 🐛 Phase 6 体验修复：①强情绪自动创建情绪记忆（纯对话轮 user + 强情绪 → `add_memory_event` subtype=emotion，title=情绪，content=情绪短句；`_STRONG_MOODS` 白名单 + 工具轮 `is_tool_round` 拦截防误建；弱情绪不建）②`get_related_memories` 结构化返回（content + strength 强中弱 + reasons）+ 预路由 (J) 关联显示改用内容并标注强度原因 |
| v3.12.0 | 2026-08-16 | ⚡ 记忆系统 Phase 6（情绪检测 + 记忆关联）：`detect_mood` 关键词优先 + LLM 兜底（9 类情绪：开心/难过/生气/焦虑/平静/兴奋/疲惫/专注/困惑）+ `set_mood_llm_fn` 模块级 LLM 钩子 + 记忆关联知识图谱（`find_related_memories` 同实体/同标签/时间临近 → 强1.0/中0.7/弱0.4 / `link_memories` additive 双向写 `related_ids` / `build_association_network` 每日批量重建 / `get_related_memories` 查询关联）+ `add_memory_event`/`add_dialogue_record`(user 轮) 自动检测情绪与建立关联 + 脑.py 预路由 (J) 关联记忆查询（"咖啡和什么相关"→展示关联记忆） |
| v3.11.1 | 2026-08-16 | 🐛 天气路由修复："今天天气怎么样"不再被 3b 判为 query→财务查询；新增天气→chat 确定性预路由（`_WEATHER_RE` 触发 + `_WEATHER_EXCL_RE` 排除花了/消费/日程等强意图）+ `_ANALYSIS_EXCL_RE` 追加天气关键词（修复"这个月下雨多吗"被分析预路由劫持）+ 3b chat 触发词扩展 |
| v3.11.0 | 2026-08-15 | ⚡ 记忆系统 Phase 5（对比分析）：跨期对比（本周/本月 vs 上周/上月 + 百分比）+ 分类对比/汇总 + 最大开支 + 高频活动 + 消费时段分布 + 周总结。`记忆引擎.run_analysis` 统一下发（关键词→函数→自然语言回复）+ 脑.py「分析」意图四级路由（确定性预路由 `_ANALYSIS_RE` / 3b `analyze` / 7b 工具白名单 / 降级关键词），分析词优先于财务词，无数据友好提示"这段时间还没有数据记录哦" |
| v3.10.11 | 2026-08-15 | 🐛 记忆写入一致性：所有记忆创建路径（add_memory_event/fact/pattern）创建即补齐 confidence/tier/retrieved_count/last_retrieved，置信度机制一致生效 |
| v3.10.10 | 2026-08-15 | 🐛 幻觉记账修复：保存后重载验证 record_id 已落盘 + save_data 失败返回"保存失败请重试"（绝不返回成功）+ 记忆写入失败时回复提示"记忆记录同步失败" |
| v3.10.9 | 2026-08-14 | 🐛 Phase 4 测试修复：财务记忆写入硬化（返回结果 + 失败 traceback 可见 + memory_event 状态暴露）+ 搜索噪音过滤（跳过 工具操作：/确认：🔍 搜索自噪音）→ 置信度机制数据充足可观测 |
| v3.10.8 | 2026-08-13 | ⚡ 记忆系统 Phase 4：置信度生命周期（tier L1/L2 + confidence 补齐 + last_retrieved 检索跟踪）+ 增量日衰减(-0.005/天) + L1→L2 提升（conf≥0.8 或实体主题频次≥3）+ 归档（conf<0.2 或 90天未检索）+ 检索提升(+0.05) + 每日自动维护 |
| v3.10.7 | 2026-08-13 | ⚡ 记忆系统 Phase 3：标签自动生成（实体+类型标签 + backfill 补齐历史）+ 时间索引（周X/上周X/自定义范围/时段）+ 搜索接口（tags/time/keyword 搜记忆+对话归档）+ 预路由 (I) 时间回查 |
| v3.10.6 | 2026-08-12 | 🐛 展开目标选择修复（候选关键词提取 + user 记录优先 + 跳过同查询） |
| v3.10.5 | 2026-08-12 | 🐛 展开触发变体增强（详细跟/详细给 + 说讲互换，不再误判 clarify） |
| v3.10.4 | 2026-08-12 | 🐛 Phase2 修复合集：消费带金额→财务 + 展开触发增强 + 展开返回原文 + 提醒不截断 |
| v3.10.3 | 2026-08-12 | 🐛 展开预路由触发增强（结构化模式 + 无记录回退正常路由 + DEBUG 日志） |
| v3.10.2 | 2026-08-12 | ⚡ 记忆系统 Phase 2：渐进披露上下文注入（轻量摘要省93% + expand_dialogue 按需展开 + recall 走对话归档） |
| v3.10.1 | 2026-08-12 | 🐛 路由修复：观点→chat / 无金额记账→chat / "有+日程词"→日程 |
| v3.10.0 | 2026-08-11 | 📚 记忆系统 Phase 1：对话归档与摘要（对话归档.json + 三级摘要策略） |
| v3.9.30 | 2026-08-08 | 🧠 记忆路由修复：记住/查看记忆/搜索 → memory 意图 + 身份问题 → chat |
| v3.9.29 | 2026-08-07 | 🧠 记忆系统存储规范化：15 字段统一格式 memory.json + add_memory_event + 工具模块集成 |
| v3.9.28 | 2026-08-07 | 🐛 日程模块修复：删除序号与展示顺序一致 + 自然日程表达 + 日期无时间全天事件 |
| v3.9.27 | 2026-08-07 | 🛡️ 路由鲁棒性修复：确定性预路由 + 会议→schedule + 一会儿强制5分钟 + 删除待办N前置拦截 + 本周查询兜底 |
| v3.9.26 | 2026-08-06 | 🐛 路由与执行修复：任务/待办→schedule + 删除待办N按序号 + 事件查询 + 本周筛选 + 一会儿→5分钟 |
| v3.9.25 | 2026-08-05 | 📅 日程模块存储规范化：15 字段标准格式 + 时段标签 + 内容生成 + 排序 + 自动迁移 |
| v3.9.24 | 2026-08-05 | 💰 财务记录标准化：15 字段标准格式 + 数据迁移 + 明细模式 |
| v3.9.23 | 2026-08-04 | 💬 统一回复层重写：工具结果转温暖回复 + 自动追问 |
| v3.9.21 | 2026-08-03 | 🗣️ chat 意图扩展触发词 + 优先级规则 |
| v3.9.18 | 2026-08-02 | 🔗 财务查询上下文继承 |
| v3.9.12 | 2026-07-31 | 📅 财务日期解析改为 LLM 优先 |
| v3.9.0 | 2026-07-29 | ⚡ 3b 紧凑路由优先，7b 降级兜底 |
| v3.8.7 | 2026-07-23 | 🧹 路由死代码清理（移除 TOOLS 列表） |
| v3.8.0 | 2026-07-21 | 🏗️ 财务模块分层架构重构 |
| v3.7.8 | 2026-07-19 | 💰 财务模块中文数字解析 |
| v3.7.7 | 2026-07-15 | 🎤 语音识别优化：AGC + 动态 VAD |
| v3.7.1 | 2026-07-05 | 拟态前端分离 + 记忆系统全面测试通过（83/83） |
| v3.7.0 | 2026-06-24 | 记忆系统主动学习升级（六步闭环） |
| v3.6.0 | 2026-06-22 | 文档体系重构 |
| v3.5.0 | 2026-06-22 | 规律遗忘机制 |
| v3.3.0 | 2026-06-22 | 数据提炼引擎 |
| v3.2.0 | 2026-06-22 | 模糊删除 + 路由优化 |

完整历史 → [`docs/03_版本历史/`](docs/03_版本历史/)
